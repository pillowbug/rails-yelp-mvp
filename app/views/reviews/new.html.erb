<div class="container d-flex justify-content-center align-items-center">
  <div class="card p-3 m-5 w-75">
    <h1>Leave a review, bitch</h1>
    <%= simple_form_for [@restaurant, @review] do |f| %>
        <%= f.input :content %>
        <%= f.input :rating %>
        <%= f.submit  %>
      <% end %>
    <%= link_to "Go home", restaurants_path %>
  </div>
</div>
